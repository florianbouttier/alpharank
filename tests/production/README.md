# Tests de production

Ce dossier couvre les workflows mensuels, la publication et la promotion sur
des fixtures contrôlées. Ces tests ne publient aucun artefact réel et ne sont
pas une autorisation de promotion.

La suite se lance avec `python -m pytest -m production`.
