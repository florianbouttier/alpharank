# Tests de replay

Ce dossier protège les snapshots immuables, la causalité, la lignée et la
parité économique. Les fixtures doivent être petites, scellées ou construites
dans un répertoire temporaire.

La suite se lance avec `python -m pytest -m replay`.
